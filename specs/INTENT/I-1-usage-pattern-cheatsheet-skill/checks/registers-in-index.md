# Agent check: every written cheatsheet is registered as an entry in the repository-root index.md

## SHALL
**WHEN** a cheatsheet file is written **THE SYSTEM SHALL** add an entry for it to the repository-root index.md. [test: agent:specs/INTENT/I-1-usage-pattern-cheatsheet-skill/checks/registers-in-index.md]

## Success criteria
_Enrich this section with concrete pass conditions: what files to inspect, what patterns count as satisfaction, what file:line evidence the subagent should cite. The seeded SHALL alone is intentionally minimal — `spec-check` will run against whatever is here, so the more specific the better._
